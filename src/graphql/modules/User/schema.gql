type User {
    id: ID
    name: String,
    email: String,
    tel: String,
    profile: Profile,
}

type Query {
    user(id: Int): User
    users: [User]
}

type Mutation {
    createUser(
        name: String,
        email: String,
        tel: String
    ): User!
}